import { TableFilterUserPipe } from './table-filter-user.pipe';

describe('TableFilterUserPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterUserPipe();
    expect(pipe).toBeTruthy();
  });
});
