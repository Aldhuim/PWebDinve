import { TableFilterUserCorreoPipe } from './table-filter-user-correo.pipe';

describe('TableFilterUserCorreoPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterUserCorreoPipe();
    expect(pipe).toBeTruthy();
  });
});
