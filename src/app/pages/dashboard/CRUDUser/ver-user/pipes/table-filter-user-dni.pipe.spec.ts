import { TableFilterUserDniPipe } from './table-filter-user-dni.pipe';

describe('TableFilterUserDniPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterUserDniPipe();
    expect(pipe).toBeTruthy();
  });
});
