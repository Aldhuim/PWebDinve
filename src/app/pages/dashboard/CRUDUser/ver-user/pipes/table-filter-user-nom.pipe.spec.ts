import { TableFilterUserNomPipe } from './table-filter-user-nom.pipe';

describe('TableFilterUserNomPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterUserNomPipe();
    expect(pipe).toBeTruthy();
  });
});
